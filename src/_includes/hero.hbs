<div class="under-nav {{pageClass}}">
  {{> ../_includes/site_nav}}
  <div class="content">
    <h1 class="hero-title">{{title}}</h1>
    {{#if logo}}
      <img src="{{logo}}">
    {{/if}}
    <p class="hero-description">{{description}}</p>
    {{#if button}}
      <button class="lego-btn">{{button}}</button>
    {{/if}}
  </div>
</div>